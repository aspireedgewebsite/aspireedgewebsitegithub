# Leave unset for local dev (Vite proxy handles /api -> http://localhost:5000).
# In production, set to your deployed backend URL, no trailing slash.
# Example: VITE_API_URL=https://aspireedge-api.onrender.com
VITE_API_URL=